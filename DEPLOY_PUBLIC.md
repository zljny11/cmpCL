# 公网部署说明

目标：将当前部署在公司内网 `192.168.1.47:8088` 的系统开放给公网用户使用，同时避免大文件上传下载受阿里云入口机带宽限制。

当前推荐的正式方案是：

- 业务入口走阿里云 `2c2g ECS + Nginx + frp`
- DICOM、模型文件等大文件统一走阿里云 OSS
- 内网服务器 `192.168.1.47` 负责业务处理、文件解析、元数据管理和下载授权

## 一、架构结论

不再使用“阿里云入口机中转大文件”的方式。

原因：

- `2c2g ECS` 带宽不适合长期承接大文件上传下载
- `frp` 适合业务小流量转发，不适合正式承接大文件传输
- 公司当前没有可稳定承载大流量的公网入口

因此正式公网架构调整为：

```text
业务小流量
公网用户
  |
https://aicampcloud.radynhealth.com
  |
阿里云 2c2g ECS + Nginx
  |
frps <=====> frpc
              |
      192.168.1.47:8088

大文件流量
用户端 / 管理侧 / 内网服务器
  |
  +---- 上传 DICOM / 模型文件 ----> 阿里云 OSS
  |
  +---- 下载 DICOM / 模型文件 ----> 阿里云 OSS

控制链路
用户端 / 管理侧
  |
  +---- 获取上传凭证 ----> 2c2g ECS / 业务后端
  |
  +---- 上传完成确认 ----> 业务后端
  |
内网服务器 192.168.1.47
  |
  +---- 解析 OSS 文件
  +---- 签发下载授权
  +---- 管理业务元数据
```

## 二、各组件职责

### 1. 阿里云 2c2g ECS

职责：

- 公网业务访问入口
- Nginx 反向代理
- `frps` 服务端
- 登录、列表、查询、轻量业务 API
- 上传凭证签发入口或上传凭证转发入口

不负责：

- 大文件实际存储
- 大文件上传下载中转
- 长时间文件流式传输

### 2. 阿里云 OSS

职责：

- 统一存储 DICOM 文件
- 统一存储模型文件
- 承接公网用户直传
- 承接公网下载
- 为内网服务器提供对象读取来源

不负责：

- 业务权限判断
- 业务元数据管理
- 最终业务状态落库

### 3. 公司内网服务器 `192.168.1.47`

职责：

- 核心业务逻辑处理
- 文件元数据记录
- DICOM 解析
- 模型业务处理
- 上传完成后的状态入库
- 下载前权限校验
- 签发下载凭证或临时链接

不负责：

- 直接承接公网大文件入口

### 4. 用户端 / 管理侧

职责：

- 先获取上传凭证
- 直接上传文件到 OSS
- 上传成功后回传文件标识给业务系统
- 下载时先请求授权，再从 OSS 直连下载

## 三、推荐数据流

### 1. DICOM 上传流程

```text
1. 用户登录业务系统
2. 前端向业务后端申请上传凭证
3. 后端返回 OSS 直传凭证 / STS / 签名
4. 前端使用凭证直接上传 DICOM 到 OSS
5. 上传成功后，前端把 objectKey / fileId / 大小 / 文件名 / 业务ID 回传后端
6. 后端写入文件元数据，并将状态标记为 uploaded
7. 内网服务器异步解析 OSS 中的 DICOM 文件
8. 解析完成后更新业务状态
```

### 2. 模型文件上传流程

```text
1. 管理侧登录后台
2. 管理侧向业务后端申请模型文件上传凭证
3. 后端返回 OSS 直传凭证
4. 管理侧直接上传模型文件到 OSS
5. 上传完成后回传 objectKey / 版本 / 文件信息
6. 后端记录模型文件元数据和版本关系
```

### 3. 文件下载流程

```text
1. 用户或客户端请求下载文件
2. 业务后端先校验权限
3. 校验通过后，后端签发 OSS 临时下载地址或下载凭证
4. 用户或客户端直接从 OSS 下载文件
```

### 4. 内网解析流程

```text
1. 后端收到上传完成确认
2. 将文件放入待解析队列或待处理状态
3. 内网服务器 192.168.1.47 拉取 OSS 文件
4. 完成 DICOM 解析、入库、索引或业务处理
5. 更新文件状态和业务结果
```

## 四、域名与入口规划

建议至少保留一个业务域名：

- `aicampcloud.radynhealth.com`
  - 页面访问
  - 登录
  - 普通 API
  - 上传凭证申请
  - 下载授权申请

如果后续需要，也可以增加专门文件域名，但在 OSS 直传模式下不是必须。

## 五、阿里云侧部署

### 1. ECS 侧职责

ECS 只承接业务流量，不承接大文件上传下载。

### 2. 端口规划

阿里云服务器开放：

- `80`
- `443`
- `7000`

不要开放：

- `9000`
- `7500`

### 3. frps 配置

`/etc/frp/frps.toml`

```toml
bindPort = 7000

auth.method = "token"
auth.token = "replace-with-a-strong-random-token"

webServer.addr = "127.0.0.1"
webServer.port = 7500
webServer.user = "admin"
webServer.password = "replace-with-a-strong-password"
```

### 4. frpc 配置

内网服务器 `192.168.1.47`：

`/etc/frp/frpc.toml`

```toml
serverAddr = "47.101.135.245"
serverPort = 7000

auth.method = "token"
auth.token = "replace-with-a-strong-random-token"

[[proxies]]
name = "campcloud-web"
type = "tcp"
localIP = "127.0.0.1"
localPort = 8088
remotePort = 9000
```

如果应用监听在 `192.168.1.47:8088`，把 `localIP` 改成 `192.168.1.47`。

### 5. Nginx 配置

阿里云入口机仅服务业务域名：

`/etc/nginx/conf.d/aicampcloud.conf`

```nginx
server {
    listen 80;
    server_name aicampcloud.radynhealth.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name aicampcloud.radynhealth.com;

    ssl_certificate /etc/letsencrypt/live/aicampcloud.radynhealth.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/aicampcloud.radynhealth.com/privkey.pem;

    proxy_connect_timeout 60s;
    proxy_send_timeout 120s;
    proxy_read_timeout 120s;
    send_timeout 120s;

    location / {
        proxy_pass http://127.0.0.1:9000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}
```

说明：

- 不要继续把大文件上传接口长期挂在这台 ECS 上做文件中转
- ECS 只做业务入口和轻量控制请求

## 六、OSS 方案设计

### 1. 存储规划

当前已确定的 Bucket：

```text
Bucket 名称：radyn-aicampcloud-file
读写权限：私有
阻止公共访问：开启
服务端加密：OSS 完全托管
加密算法：AES256
```

当前正式采用以下对象前缀规则：

```text
dicom/incoming/
dicom/parsed/
models/
temp/
```

说明：

- OSS 中的“目录”本质上是对象 Key 前缀，不要求先在控制台手工创建真实目录
- 后续对象只要按下述命名规则上传，对应前缀会自然形成
- 除非后续明确引入多租户隔离，否则当前统一按 `requirementId` 作为业务分组维度
- `temp/` 仅用于短期中间文件、调试产物或分片上传临时对象，不作为长期业务归档目录

### 2. 命名建议

建议不要直接使用用户原始文件名作为 OSS 对象 Key。

当前正式命名规则：

```text
dicom/incoming/{requirementId}/{uuid}.dcm
dicom/parsed/{requirementId}/{uuid}.json
models/{modelName}/{version}/{uuid}.bin
temp/{requirementId}/{uuid}
```

命名约束：

- `requirementId` 指业务需求单 ID，作为当前 DICOM 文件的主业务归属键
- `uuid` 必须由后端生成或由后端签发上传凭证时下发，前端不能自行拼接最终对象 Key
- `modelName`、`version` 由业务后端校验后写入，不能由客户端自由决定任意前缀
- 用户原始文件名只存数据库字段，例如 `originalFileName`，不直接进入对象 Key 主体
- 对象 Key 一经确认后，上传完成确认、解析任务、下载授权都统一以 `objectKey` 为准，不再依赖原始文件名定位对象

### 3. 上传方式

推荐：

- 前端直传 OSS
- 使用 STS 临时凭证或服务端签名
- 大文件使用分片上传

当前访问边界要求：

- 前端和管理端不能持有永久 AccessKey
- 上传凭证必须由业务后端签发
- 上传权限必须限制到允许的前缀范围，不能给整个 Bucket 任意写权限
- 上传完成后必须调用业务后端“上传完成确认”接口，不能仅以 OSS 中存在对象作为业务成功依据
- 如需支持断点续传或大文件上传，优先采用 OSS 分片上传，不再走 ECS 或业务后端文件中转

### 4. 下载方式

推荐：

- 不暴露永久公网 URL
- 后端校验权限后生成临时下载地址
- 临时 URL 设置较短有效期

当前访问边界要求：

- Bucket 保持私有，不允许公共读
- 下载必须先经过业务后端权限校验
- 业务后端只返回短时有效的临时下载 URL，不返回永久直链
- 临时下载 URL 应限制有效期，建议默认控制在分钟级到小时级，不应长期有效
- 对象是否允许下载，以业务库中的文件状态、业务归属和当前用户权限为准，不以 OSS 中对象存在与否直接决定

### 5. 解析方式

推荐：

- 上传完成后写入数据库状态
- 内网服务器异步拉取 OSS 文件
- 完成解析后更新状态

当前约束：

- `192.168.1.47` 负责从 OSS 拉取 `dicom/incoming/` 下的业务对象
- 解析产物如需持久化到 OSS，统一写入 `dicom/parsed/`
- 解析任务的状态流转必须落库，至少包括 `pending_upload / uploaded / parsing / parsed / failed`
- `temp/` 下的临时对象不能替代正式业务状态，必要时应设置后续生命周期清理策略

## 七、应用改造说明

这个方案需要前后端做一定改造，但通常不是重写系统。

### 1. 前端改造

前端需要新增：

- 获取上传凭证接口调用
- 直传 OSS 逻辑
- 上传成功后的确认回调
- 下载前先申请下载授权

如果当前前端已有统一请求封装，改造一般集中在上传下载模块。

### 2. 后端改造

后端需要新增或调整：

- 上传凭证签发接口
- 上传完成确认接口
- 文件元数据表
- 文件状态字段
- 下载授权接口
- 异步解析任务或轮询处理逻辑

### 3. 建议新增的文件状态

例如：

- `pending_upload`
- `uploaded`
- `parsing`
- `parsed`
- `failed`

## 八、最关键的业务要求确认

这个方案成立的前提是：

**公司接受 DICOM 文件和模型文件存放在阿里云 OSS。**

如果公司不接受“文件上云”，那这个方案就不成立，只能继续推动公司自建正式公网文件入口。

## 九、优点与结果

### 优点

- 大文件上传下载不再经过 `2c2g ECS`
- 不依赖公司提供高带宽公网入口
- `192.168.1.47` 不用直接承受公网大流量
- 架构更接近云上标准做法
- 后续支持分片上传、断点续传、生命周期管理更容易

### 结果

- 2c2g ECS 足够承接业务小流量
- OSS 承担大文件存储和传输
- 内网服务器专注业务处理和解析

## 十、最低限度安全要求

1. 所有外部访问必须走 HTTPS
2. OSS 不应直接裸公开读写
3. 上传必须使用临时凭证或服务端签名
4. 下载必须先做业务权限校验
5. 不返回永久下载链接
6. 上传完成后必须有业务确认，不要只依赖 OSS 里“有文件”
7. 记录上传、下载、解析日志

## 十一、最短落地顺序

1. 保留并继续使用已打通的 `ECS + Nginx + frp` 业务链路
2. 将 `aicampcloud.radynhealth.com` 指向阿里云 ECS
3. 开通 OSS Bucket
4. 后端实现上传凭证签发接口
5. 前端改为直传 OSS
6. 后端实现上传完成确认接口
7. 内网服务器实现 OSS 文件异步解析
8. 后端实现下载授权接口
9. 联调 DICOM 上传、模型上传、文件下载、解析回写

## 十一A、推荐实施阶段（串行，不并行）

虽然整体工作常被口头概括为“4 个阶段”，但按实际执行风险看，联调验收最好单独作为最后一段。

如果必须严格压成 4 个阶段，可将“第 5 阶段：联调验证”并入“第 4 阶段：前端改上传下载”的收尾部分；但执行顺序仍然必须保持串行，不建议提前混做。

### 第 1 阶段：先把现有业务公网入口稳定住

目标：

- 先确认“小流量业务访问已可公网使用”
- 这一阶段不以文件上传下载为目标

检查项：

- 确认阿里云 `2c2g ECS` 上 `frps` 正常运行
- 确认阿里云 `2c2g ECS` 上 `nginx` 正常运行
- 确认 `127.0.0.1:9000` 能访问到内网应用
- 配好业务域名：`aicampcloud.radynhealth.com -> 阿里云 ECS 公网 IP`
- 配好业务域名 HTTPS
- 使用 `certbot --nginx -d aicampcloud.radynhealth.com`
- 验证业务小流量链路：
  - 打开首页
  - 登录
  - 列表、查询、表单提交

验收标准：

- 用户可通过 `https://aicampcloud.radynhealth.com` 正常访问现有业务
- 业务链路稳定可用，但大文件链路暂不切换

### 第 2 阶段：开通 OSS 并确定存储规则

目标：

- 先把“文件怎么存、存哪、谁能传、谁能下”定清楚

检查项：

- 在阿里云开通 OSS Bucket：`radyn-aicampcloud-file`
- 确定 Bucket 所在地域，尽量与 ECS 接近
- 确定 Bucket 权限与安全参数：
  - 读写权限：私有
  - 阻止公共访问：开启
  - 服务端加密：OSS 完全托管
  - 加密算法：AES256
- 确定目录前缀：
  - `dicom/incoming/`
  - `dicom/parsed/`
  - `models/`
  - `temp/`
- 确定对象命名规则：
  - `dicom/incoming/{requirementId}/{uuid}.dcm`
  - `dicom/parsed/{requirementId}/{uuid}.json`
  - `models/{modelName}/{version}/{uuid}.bin`
  - `temp/{requirementId}/{uuid}`
- 确定访问策略：
  - 不允许公共读写
  - 上传仅允许临时凭证或服务端签名
  - 上传权限必须限制到允许前缀
  - 下载走临时授权 URL
  - 不返回永久下载直链

验收标准：

- OSS Bucket、目录规则、命名规则、访问策略都已确定
- 不再依赖“后续再看”的临时存储方案

### 第 3 阶段：后端先补 4 个接口

目标：

- 让业务系统先“认识 OSS 文件”
- 前端改造之前，先把后端联调能力补齐

后端至少需要补齐：

- 上传凭证签发接口
  - 返回 OSS 直传凭证或 STS
- 上传完成确认接口
  - 前端上传成功后回传 `objectKey / fileName / size / bizId`
- 下载授权接口
  - 校验权限后返回临时下载 URL
- 文件元数据和状态管理
  - 建文件表
  - 状态字段至少包含：
    - `pending_upload`
    - `uploaded`
    - `parsing`
    - `parsed`
    - `failed`

同时需要补一条后台处理链路：

1. `192.168.1.47` 从 OSS 拉文件
2. 异步解析 DICOM
3. 回写文件状态和业务结果

验收标准：

- 后端已经具备 OSS 文件接入、登记、授权、状态流转能力
- 文件不再只是“躺在 OSS 里”

### 第 4 阶段：前端改上传下载

目标：

- 让大文件流量真正从业务后端剥离

前端改造重点：

- 上传：
  - 不再把大文件传给业务后端
  - 先请求上传凭证
  - 再直传 OSS
  - 成功后调用“上传完成确认”
- 下载：
  - 不再直接请求后端流式下载
  - 先请求下载授权
  - 再跳转或拉取 OSS 临时下载地址
- 配置：
  - 业务 API 仍走 `aicampcloud.radynhealth.com`

验收标准：

- 前端上传下载链路已切换到 OSS
- 业务后端只负责控制请求和权限判断

### 第 5 阶段：联调验证

目标：

- 按顺序验证，不要一上来就全流程压测

建议顺序：

1. 业务域名访问正常
2. 上传凭证接口正常
3. 小文件直传 OSS 成功
4. 上传完成确认成功
5. 数据库里有文件记录
6. `192.168.1.47` 能拉到 OSS 文件并解析
7. 下载授权正常
8. 临时下载 URL 可用
9. 再测更大的 DICOM 或 ZIP

验收标准：

- 业务小流量入口稳定
- 大文件流量已切到 OSS
- 内网服务器仅承担业务处理、解析、授权和状态回写

### 当前最实际的执行顺序

1. 收尾 ECS 业务域名和 HTTPS
2. 开通 OSS Bucket
3. 设计文件表和状态字段
4. 先做后端 4 个接口
5. 再改前端上传下载
6. 最后做 OSS 解析联调

一句话总结：

- 先保住现有 `ECS + frp` 的业务入口
- 再把大文件链路切到 OSS 直传直下
- 最后让 `192.168.1.47` 专注业务处理和解析

## 十二、常见风险

### 1. 前端仍然把文件传给业务后端

这样会导致大文件仍然压在 ECS 和 `frp` 上，等于没有真正切换成功。

### 2. 只上传到 OSS，不做业务确认

这样会出现 OSS 有文件，但业务库没有记录，最终造成脏数据和孤儿文件。

### 3. 直接返回永久 URL

这样权限不可控，文件容易被长期传播。

### 4. 内网解析没有异步状态管理

解析失败、重试、超时会难以追踪。

## 十三、与现有内网部署的关系

如果你已经按 [DEPLOY_INTRANET.md](/Users/zljny/code/radyn-workspace/DEPLOY_INTRANET.md) 在内网把项目跑起来，这份文档是在那个基础上增加正式公网访问方案。

保持不变的部分：

- 内网应用主体仍在 `192.168.1.47:8088`
- `frp` 业务链路仍可继续使用

变化的部分：

- 不再用 ECS 中转大文件
- 大文件统一改为 OSS 直传直下
- 前后端增加文件凭证、确认、解析、授权逻辑

## 十四、当前正式推荐方案

当前最推荐的正式公网架构是：

- 业务小流量走 `阿里云 2c2g ECS + Nginx + frp`
- DICOM 与模型文件走 `阿里云 OSS`
- 内网服务器 `192.168.1.47` 负责业务处理、文件解析、元数据和下载授权

这是当前在“公司没有高带宽公网入口”的前提下，最现实、最稳、最可长期演进的方案。
